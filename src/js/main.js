import { ImageCarousel } from './carousel.js';
import { PaymentSelector } from './payment.js';

document.addEventListener('DOMContentLoaded', () => {
    new ImageCarousel();
    new PaymentSelector();
});